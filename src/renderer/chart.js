// chart.js
// simple chart / graph functions
// not to be confused with the chart proper / chart panes class

import { renderRect } from "./rect";

export function chartBar(ctx) {

  renderRect (ctx, x, y, w, h, opts)
}


export function chartLine(ctx) {

}

export function chartPie(ctx) {

}
